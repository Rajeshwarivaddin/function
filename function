#include<stdio.h>
int max(int num1,int num2);
int main()
{
	int a=1000;
	int b=200;
	int rat;
	rat=max(a,b);
	printf("max value is=%d\n",rat);
	return 0;
	
}
int max(int num1,int num2)

{
	int result;
	if(num1>num2)
	result=num1;
	else
   result=num2;
   return result;
}
